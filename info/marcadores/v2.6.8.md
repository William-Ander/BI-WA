# BI WA v2.6.8

## Tipo
Correção de instalação / ícone oficial.

## Alterações
- Mantém exatamente o arquivo `BI WA(3).ico` como ícone oficial do app.
- Gera PNGs técnicos a partir do próprio ICO oficial apenas para o navegador/manifest, sem mudar o desenho do ícone.
- Cria novo perfil local do navegador: `browser-profile-biwa-oficial-v268`.
- Remove perfis antigos `browser-profile-biwa*` para evitar cache do ícone antigo.
- Recria o atalho `BI WA.lnk` apontando para `public/app-icon.ico`.
- Tenta atualizar o cache visual do Windows com `ie4uinit.exe -show`.

## Regra permanente
Não redesenhar, simplificar ou substituir o ícone oficial. Usar sempre o ICO enviado pelo usuário como fonte.
