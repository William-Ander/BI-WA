# BI WA v3.2.33

Correção do instalador único no Windows.

- Mantido apenas `instalar_bi_wa.bat`.
- Corrigida chamada de instalação para usar `npm.cmd` diretamente.
- Adicionado fallback via `cmd.exe`.
- Evita erro `Unknown command: "pm"` durante `npm install`.
- Mantido pacote limpo, sem `.env` real, `node_modules`, logs, perfis locais ou credenciais reais.
