# v3.2.41 - Atualização instantânea dos visuais de tabela

- Campo marcado/arrastado aparece no visual imediatamente.
- Tabela/matriz aceitam múltiplas colunas selecionadas.
- Backend `/api/visual-query` passa a aceitar `columns` para consulta direta de tabela.
- Mantida preservação da pasta `data` e `.env` nas atualizações.
